{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build && echo 'Laravel API Build Complete'",
        "dev": "vite",
        "postinstall": "npm run build",
        "start": "php artisan serve --host 0.0.0.0 --port ${PORT:-8000}"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.8.2",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.2.0",
        "tailwindcss": "^4.0.0",
        "vite": "^6.2.4"
    },
    "engines": {
        "node": ">=16.0.0",
        "yarn": ">=1.22.0"
    }
}
